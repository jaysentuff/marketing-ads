{
  "name": "marketing-ads",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "shopify": "shopify",
    "dev": "shopify app dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run start"
  }
}
